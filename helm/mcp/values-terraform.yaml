# terraform-only release in namespace mcp-terraform.
tunnel:
  enabled: false

serviceAccount:
  create: false

servers:
  terraform-stacks:
    enabled: false
  google-cloud:
    enabled: false
  whatsapp-business:
    enabled: false
