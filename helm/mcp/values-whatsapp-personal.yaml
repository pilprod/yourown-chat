# Personal QR-linked WhatsApp MCP only.
tunnel:
  enabled: false

serviceAccount:
  create: false

servers:
  terraform:
    enabled: false
  terraform-stacks:
    enabled: false
  google-cloud:
    enabled: false
  whatsapp-business:
    enabled: false
