# whatsapp-business-only release in namespace mcp-whatsapp-business.
tunnel:
  enabled: false

serviceAccount:
  create: false

servers:
  terraform:
    enabled: false
  google-cloud:
    enabled: false
  google-workspace:
    enabled: false
