# terraform-stacks-only release in namespace mcp-terraform-stacks.
tunnel:
  enabled: false

serviceAccount:
  create: false

servers:
  terraform:
    enabled: false
  google-cloud:
    enabled: false
  whatsapp-business:
    enabled: false
  whatsapp-personal:
    enabled: false
