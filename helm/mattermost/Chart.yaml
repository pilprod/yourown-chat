apiVersion: v2
name: mattermost
description: Mattermost application chart promoted from dev migration tests to production.
type: application
version: 0.1.0
# The image-triggered release substitutes the exact immutable v*-patched tag
# in its frozen Cloud Deploy source checkout.
appVersion: "from-image-trigger"
