apiVersion: v2
name: identity
description: Identity services release wrapper (test fixture). Pins platform profiles and carries only service values and environment overlays.
type: application
version: 0.1.0
dependencies:
  # Tests rewrite these file:// references to the absolute platform checkout
  # and regenerate Chart.lock; a real wrapper pins oci://<platform registry>.
  - name: platform-service
    version: 0.1.0
    repository: file://../../../../../../platform/platform-service
    alias: identity-api
  - name: platform-job
    version: 0.1.0
    repository: file://../../../../../../platform/platform-job
    alias: identity-migrate
