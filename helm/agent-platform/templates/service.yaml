apiVersion: v1
kind: Service
metadata:
  name: yourown-chat-control-api
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "agent-platform.labels" . | nindent 4 }}
spec:
  selector: { app: yourown-chat-control-api }
  ports:
    - name: http
      port: 8080
      targetPort: http
