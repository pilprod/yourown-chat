{{- if .Values.serviceAccount.create }}
# Shared Workload Identity KSA for servers needing keyless GCP access, bound to
# the platform `mcp` GSA. This is rendered only in the mcp-google-cloud
# namespace. Annotation value injected via the mcp_gsa deploy parameter.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name }}
  labels:
    app.kubernetes.io/part-of: mcp-servers
  annotations:
    iam.gke.io/gcp-service-account: "from-deploy-parameters" # from-param: ${mcp_gsa}
{{- end }}
