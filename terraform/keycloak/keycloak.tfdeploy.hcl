# Transitional empty deployment configuration. The Keycloak resources and
# component state are gone; applying this revision removes the final HCP Stack
# deployment metadata so the Stack itself can be deleted.
