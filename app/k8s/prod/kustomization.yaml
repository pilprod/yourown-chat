apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Prod overlay: sample-prod namespace, base replica count (2). Promotion into
# this stage is gated by manual approval on the Cloud Deploy target.
namespace: sample-prod

resources:
  - namespace.yaml
  - ../base
