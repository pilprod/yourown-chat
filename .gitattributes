# Unified diffs intentionally contain leading `+` markers and preserve the
# upstream file's indentation; do not interpret their payload as this repo's
# own whitespace when running `git diff --check`.
helm/mattermost/rtcd/dependencies.patch whitespace=-blank-at-eol,-blank-at-eof,-space-before-tab
